apiVersion: v1
kind: Service
metadata:
  name: {{ include "intsall-acs.fullname" . }}
  labels:
    {{- include "intsall-acs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "intsall-acs.selectorLabels" . | nindent 4 }}
