apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "intsall-kyverno.fullname" . }}-test-connection"
  labels:
    {{- include "intsall-kyverno.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "intsall-kyverno.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
