apiVersion: v1
kind: Service
metadata:
  name: {{ include "intsall-kyverno.fullname" . }}
  labels:
    {{- include "intsall-kyverno.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "intsall-kyverno.selectorLabels" . | nindent 4 }}
