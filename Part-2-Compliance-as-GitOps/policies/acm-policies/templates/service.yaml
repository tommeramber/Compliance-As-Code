apiVersion: v1
kind: Service
metadata:
  name: {{ include "acm-policies.fullname" . }}
  labels:
    {{- include "acm-policies.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acm-policies.selectorLabels" . | nindent 4 }}
