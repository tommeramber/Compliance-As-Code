apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acm-policies.fullname" . }}-test-connection"
  labels:
    {{- include "acm-policies.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acm-policies.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
