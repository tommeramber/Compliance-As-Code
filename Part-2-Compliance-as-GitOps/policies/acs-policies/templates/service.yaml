apiVersion: v1
kind: Service
metadata:
  name: {{ include "acs-policies.fullname" . }}
  labels:
    {{- include "acs-policies.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acs-policies.selectorLabels" . | nindent 4 }}
