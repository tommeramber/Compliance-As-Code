apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acs-policies.fullname" . }}-test-connection"
  labels:
    {{- include "acs-policies.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acs-policies.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
